/**
 * Componente conservado por compatibilidad.
 * La iluminación ahora vive dentro de cada HeroGridCube usando su propia config `lgt`.
 */
export default function HeroGridLights() {
  return null
}
